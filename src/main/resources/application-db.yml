spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  sql:
    init:
      # Hibernate가 geography/vector 컬럼을 생성하기 전에 확장 초기화 SQL을 실행
      mode: always
