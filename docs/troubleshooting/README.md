# Troubleshooting (문제 해결 및 기술적 도전 과제)

프로젝트 개발 과정에서 발생한 주요 문제, 원인 분석 및 해결 방안(증상 - 원인 - 해결) 정리 문서 목록입니다.

## 📄 문서 목록

1. [Refresh Token 쿠키 중복 발급 및 세션 꼬임 이슈](./Refresh_Token_쿠키_중복_이슈.md)
   - **주요 내용**: 쿠키 `Path` 변경(`Path=/auth` -> `Path=/`)으로 인한 브라우저 내 동일 쿠키 중복 발급 및 토큰 회전 오작동 해결.
2. [사용자당 중복 실시간 매칭 요청 및 동시성 이슈](./실시간_매칭_동시성_이슈.md)
   - **주요 내용**: 동시 요청 및 경쟁 조건(Race Condition)으로 인한 중복 매칭 문제를 Redis `SET NX` 분산 예약 잠금과 DB 오름차순 비관적 잠금으로 해결.
