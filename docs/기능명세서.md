## FR-01. 회원 관리

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-01-01 | 사용자는 회원가입을 할 수 있어야 한다. | 필수 | Spring Security `PasswordEncoder`로 비밀번호를 해싱한 뒤 `users` 테이블에 저장하며 원문 비밀번호는 저장하거나 로그에 남기지 않음 |
| FR-01-02 | 사용자는 로그인 및 로그아웃을 할 수 있어야 한다. | 필수 | 이메일 로그인은 `AuthenticationManager`, 카카오·Google 로그인은 OAuth2 Client로 인증한다. `users.provider`와 `provider_id`로 가입 방식을 식별하며 성공 시 자체 서명한 단기 Access Token과 교체 가능한 Refresh Token을 발급하고, 로그아웃 시 Refresh Token을 폐기 |
| FR-01-03 | 사용자는 회원탈퇴를 할 수 있어야 한다. | 필수 | `users.status = withdrawn` 처리, 개인정보 마스킹, 해당 사용자의 Refresh Token 전체 폐기 후 이후 로그인과 API 접근을 차단. 하드 삭제 시점은 데이터 보존 정책에 따라 별도 처리 |
| FR-01-04 | 사용자는 자신의 프로필 정보를 조회할 수 있어야 한다. | 필수 | Spring Boot REST API (`GET /api/users/me`), JWT의 sub 클레임으로 본인 조회만 허용 |
| FR-01-05 | 사용자는 닉네임, 프로필 이미지, 관심사 등의 정보를 수정할 수 있어야 한다. | 필수 | `PATCH /api/users/me`. 프로필 이미지는 Supabase Storage에 업로드 후 URL만 `users` 테이블에 저장 |
| FR-01-06 | 인증되지 않은 사용자는 매칭, 채팅 등 회원 전용 기능을 사용할 수 없어야 한다. | 필수 | Spring Security의 인증 필터 체인에서 JWT 미존재/만료 시 `401 Unauthorized` 반환, 회원 전용 엔드포인트는 `@PreAuthorize("isAuthenticated()")` 적용 |
| FR-01-07 | 시스템은 사용자별 권한을 구분하여 접근을 제어해야 한다. | 필수 | 서버가 발급하는 JWT에 `roles` 클레임을 포함하고 Spring Security Authority로 변환하여 `@PreAuthorize("hasRole(...)")`로 API 단위 권한 제어 |

---

## FR-02. 매칭 게시글 관리

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-02-01 | 회원은 식사 매칭을 위한 모집 게시글을 작성할 수 있어야 한다. | 필수 | `POST /api/posts`, PostgreSQL `recruit_posts` 테이블에 저장 |
| FR-02-02 | 게시글에는 모집 제목과 설명을 입력할 수 있어야 한다. | 필수 | `title`, `description` 컬럼. `description`은 저장 시 비동기로 임베딩 API 호출하여 `embedding`(pgvector) 컬럼에 함께 저장 |
| FR-02-03 | 게시글에는 식사 날짜 및 시간을 지정할 수 있어야 한다. | 필수 | `meal_datetime timestamptz` 컬럼 |
| FR-02-04 | 게시글에는 식당 또는 지역 정보를 지정할 수 있어야 한다. | 필수 | `region text` + `location geography(Point)` (PostGIS), 지도 좌표 선택 UI와 연동 |
| FR-02-05 | 게시글에는 모집 인원을 설정할 수 있어야 한다. | 필수 | `capacity int`, `current_count int` 컬럼으로 관리 |
| FR-02-06 | 모집 유형을 소규모와 대규모 등으로 구분할 수 있어야 한다. | 선택 | `recruit_type enum(small, large)` |
| FR-02-07 | 게시글의 상태를 모집중, 마감, 완료로 관리할 수 있어야 한다. | 필수 | `status enum(recruiting, closed, done)`, 상태 전이는 서버 로직에서만 허용 |
| FR-02-08 | 게시글 작성자는 자신의 모집 게시글을 수정하거나 삭제할 수 있어야 한다. | 필수 | `writer_id`와 요청자 JWT sub 비교, 불일치 시 `403 Forbidden` |
| FR-02-09 | 모집 인원이 모두 충족되면 모집 상태를 자동으로 마감 처리할 수 있어야 한다. | 선택 | 참여 신청 처리 트랜잭션 내에서 `current_count == capacity` 도달 시 `status`를 `closed`로 원자적 업데이트 |
| (검색) | 유사 게시글 검색 | 필수 | 사용자 검색어를 임베딩 → pgvector 코사인 유사도(`<=>` 연산자)로 `recruit_posts.embedding`과 비교하여 정렬 |

---

## FR-03. 실시간 매칭

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-03-01 | 회원은 실시간 매칭을 요청할 수 있어야 한다. | 필수 | `POST /api/matches/realtime`, 요청 시 Redis에 대기 상태 등록 (`match:waiting:{requestId}`) |
| FR-03-02 | 사용자는 매칭 요청 시 현재 위치 또는 원하는 지역을 설정할 수 있어야 한다. | 필수 | 위경도 파라미터를 PostGIS 조회용 `geography(Point)`로 저장 |
| FR-03-03 | 사용자는 원하는 식사 시간대를 설정할 수 있어야 한다. | 필수 | Redis 대기 정보에 `desiredTimeRange` 포함, 후보 탐색 시 시간대 겹침 여부로 필터링 |
| FR-03-04 | 사용자는 원하는 모집 인원 또는 매칭 유형을 설정할 수 있어야 한다. | 필수 | `capacityType enum(1:1, group)` 필드로 하드 필터링 |
| FR-03-05 | 시스템은 매칭 대기 중인 사용자의 상태를 관리해야 한다. | 필수 | Redis TTL로 대기 상태 자동 관리 (`waiting → matched/cancelled/expired`) |
| FR-03-06 | 시스템은 위치, 시간, 인원 등의 조건을 기반으로 적합한 사용자를 탐색해야 한다. | 필수 | **1단계(하드 필터)**: PostGIS `ST_DWithin`으로 반경 내 후보 조회 + 시간대·인원유형 조건 필터. **2단계(소프트 랭킹)**: 사용자가 입력한 "원하는 상대방 성향" 자유 텍스트를 임베딩하여 Redis Vector Search(RediSearch HNSW 인덱스)로 1단계 통과 후보군 내에서 유사도 순 정렬. 대기 인원이 적은 초기 단계에서는 브루트포스 코사인 유사도 계산으로도 충분 |
| FR-03-07 | 매칭이 성사되면 참여 사용자에게 매칭 결과를 실시간으로 전달해야 한다. | 필수 | 매칭 확정 시 WebSocket + STOMP로 `/topic/matches/{requestId}` 구독자에게 push |
| FR-03-08 | 사용자는 매칭 대기 중 요청을 취소할 수 있어야 한다. | 필수 | `DELETE /api/matches/realtime/{requestId}`, Redis 키 즉시 삭제 |
| FR-03-09 | 일정 시간이 지나도 매칭되지 않은 요청은 자동 만료될 수 있어야 한다. | 선택 | Redis 키 TTL 설정(예: 15분)으로 자동 만료 처리, 만료 시 이벤트 리스너에서 사용자에게 알림 |
| FR-03-10 | 동일 사용자가 동시에 여러 매칭에 중복 참여하지 못하도록 제한할 수 있어야 한다. | 필수 | Redis에 `match:user:{userId}` 키로 현재 대기 중인 requestId 기록, 신규 요청 시 존재 여부 확인 후 `409 Conflict` 반환 |

---

## FR-04. 위치 기능

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-04-01 | 시스템은 사용자의 위도 및 경도 정보를 기반으로 위치를 처리할 수 있어야 한다. | 필수 | PostGIS `geography(Point, 4326)` 타입으로 좌표 저장 |
| FR-04-02 | 사용자는 자신의 현재 위치 주변의 모집 게시글을 조회할 수 있어야 한다. | 필수 | `ST_DWithin(location, 현재위치, 반경)` 쿼리로 `recruit_posts` 조회 |
| FR-04-03 | 시스템은 특정 반경 내에 있는 매칭 후보를 조회할 수 있어야 한다. | 필수 | 실시간 매칭 대기열에도 동일한 PostGIS 반경 쿼리 적용 |
| FR-04-04 | 사용자는 매칭 검색 반경을 설정할 수 있어야 한다. | 선택 | `radius_km` 파라미터를 요청에 포함, 기본값(예: 3km) 제공 |
| FR-04-05 | 시스템은 사용자와 매칭 대상 간 거리를 계산할 수 있어야 한다. | 필수 | PostGIS `ST_Distance` 함수로 두 지점 간 거리 계산 후 응답에 포함 |
| FR-04-06 | 위치 정보는 사용자의 동의를 받은 경우에만 수집되어야 한다. | 필수 | `users.location_consent boolean` 플래그, 앱 최초 실행 시 동의 UI 노출. 미동의 시 위치 기반 API는 `403 Forbidden` |

---

## FR-05. 채팅

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-05-01 | 매칭이 완료된 사용자들 사이에 채팅방이 생성되어야 한다. | 필수 | 매칭 확정 트랜잭션 내에서 `chat_rooms` + `chat_room_members` 레코드 자동 생성 |
| FR-05-02 | 사용자는 채팅방에서 실시간 메시지를 전송하고 수신할 수 있어야 한다. | 필수 | WebSocket + STOMP. 클라이언트는 `/app/chat/{roomId}`로 전송, `/topic/chat/{roomId}` 구독으로 수신 |
| FR-05-03 | 사용자는 자신이 참여한 채팅방 목록을 조회할 수 있어야 한다. | 필수 | `GET /api/chatrooms`, `chat_room_members` 조인 조회 |
| FR-05-04 | 사용자는 채팅방의 이전 메시지를 조회할 수 있어야 한다. | 필수 | `GET /api/chatrooms/{roomId}/messages`, `chat_messages` 커서 기반 페이지네이션 |
| FR-05-05 | 허가되지 않은 사용자는 해당 채팅방에 접근할 수 없어야 한다. | 필수 | STOMP 연결/구독 시 `ChannelInterceptor`에서 `chat_room_members` 존재 여부 검증 |
| FR-05-06 | 매칭 종료 이후 채팅방을 종료 상태로 전환할 수 있어야 한다. | 선택 | `PATCH /api/chatrooms/{roomId}/close`, `status = closed` 처리 후 메시지 전송 차단 |

---

## FR-06. 게시판 및 후기

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-06-01 | 회원은 커뮤니티 게시글을 작성할 수 있어야 한다. | 필수 | `POST /api/community/posts`, `community_posts` 테이블 |
| FR-06-02 | 회원은 게시글 목록 및 상세 내용을 조회할 수 있어야 한다. | 필수 | `GET /api/community/posts`, `GET /api/community/posts/{id}` |
| FR-06-03 | 작성자는 자신의 게시글을 수정할 수 있어야 한다. | 필수 | 작성자 검증 후 `PATCH` 허용 |
| FR-06-04 | 작성자는 자신의 게시글을 삭제할 수 있어야 한다. | 필수 | 작성자 검증 후 `DELETE` 허용 (soft delete 권장) |
| FR-06-05 | 회원은 게시글에 댓글을 작성할 수 있어야 한다. | 선택 | `community_comments` 테이블, `POST /api/community/posts/{id}/comments` |
| FR-06-06 | 회원은 자신의 매칭 경험에 대한 후기를 작성할 수 있어야 한다. | 필수 | `community_posts.post_type = review`로 구분하여 저장 |
| FR-06-07 | 작성된 후기는 본인의 마이페이지에서 조회할 수 있어야 한다. | 필수 | `GET /api/mypage/community-posts`에서 `post_type = review` 필터 |

---

## FR-07. 사용자 후기

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-07-01 | 매칭에 실제 참여한 사용자만 상대방에게 후기를 작성할 수 있어야 한다. | 필수 | 후기 작성 시 `match_id`로 실제 참여자인지 검증(참여 기록 조회) 후 `user_reviews`에 저장. 동일 매칭에 대한 중복 작성은 unique 제약(`match_id + writer_id + target_id`)으로 차단 |
| FR-07-02 | 사용자는 다른 사용자의 공개된 매칭 후기를 조회할 수 있어야 한다. | 필수 | `GET /api/users/{userId}/reviews`, `is_public = true` 조건 필터 |
| FR-07-03 | 후기에는 상대방에 대한 간단한 평가 또는 내용을 작성할 수 있어야 한다. | 필수 | `user_reviews.content text` 컬럼, 필요 시 별도 평점(rating) 컬럼 확장 가능 |
| FR-07-04 | 사용자는 부적절한 후기를 신고할 수 있어야 한다. | 선택 | `POST /api/reviews/{reviewId}/report`, `reports` 테이블에 신고 사유 기록 후 관리자 검토 큐로 전달 |
| FR-07-05 | 사용자가 원할 경우 일부 후기를 비공개 처리할 수 있도록 정책을 설정할 수 있어야 한다. | 선택 | `PATCH /api/reviews/{reviewId}/visibility`, `is_public` 토글 (본인이 받은 후기에 한해 허용) |

---

## FR-08. AI 추천

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-08-01 | 사용자는 매칭 조건을 기반으로 식당 추천을 요청할 수 있어야 한다. | 선택 | `POST /api/ai/restaurant-recommendations`, 채팅방 대화 내용 또는 별도 입력값을 요청 본문으로 전달 |
| FR-08-02 | 시스템은 지역, 음식 종류, 인원 등의 정보를 활용하여 적절한 식당을 추천할 수 있어야 한다. | 선택 | **1단계**: Spring AI로 대화/입력 텍스트에서 지역·음식종류·인원·예산을 구조화 JSON으로 파싱 → 공공데이터/카카오맵 API에서 필터링. **2단계**: 필터링 결과 중 최소 후기 수(예: 3건) 이상 보유한 식당은 `restaurant_reviews.embedding`(pgvector)과 요청 텍스트 임베딩 간 코사인 유사도로 재정렬. 후기 부족 식당은 1단계 결과 순서 그대로 노출 |
| FR-08-03 | AI 추천 기능에 장애가 발생하더라도 기본 매칭 기능은 정상적으로 사용할 수 있어야 한다. | 필수 | AI 추천 로직을 별도 서비스/모듈로 분리, 예외 발생 시 매칭·채팅 플로우와 독립적으로 fallback(빈 결과 또는 기본 필터 결과) 반환. Circuit Breaker(Resilience4j) 적용 권장 |
| FR-08-04 | AI가 생성한 추천 결과임을 사용자에게 구분하여 표시해야 한다. | 선택 | 응답에 `is_ai_generated: true` 플래그 포함, 프론트엔드에서 "AI 추천" 배지 표시. 1단계(필터만)와 2단계(후기 기반 재정렬) 결과도 구분 표시 |

---

## FR-09. 마이페이지

| ID | 요구사항 | 우선순위 | 구현 방식 |
| --- | --- | --- | --- |
| FR-09-01 | 사용자는 자신의 프로필을 조회할 수 있어야 한다. | 필수 | `GET /api/mypage/profile`, JWT sub로 본인 데이터만 조회 |
| FR-09-02 | 사용자는 자신이 작성한 모집 게시글을 조회할 수 있어야 한다. | 필수 | `GET /api/mypage/posts`, `writer_id` 기준 조회 |
| FR-09-03 | 사용자는 자신이 참여했던 매칭 기록을 조회할 수 있어야 한다. | 필수 | `GET /api/mypage/matches`, 매칭 결과 테이블에서 참여자 기준 조회 |
| FR-09-04 | 사용자는 자신이 작성한 커뮤니티 게시글을 조회할 수 있어야 한다. | 선택 | `GET /api/mypage/community-posts` |
| FR-09-05 | 사용자는 자신이 받은 방명록 및 매칭 후기를 조회할 수 있어야 한다. | 필수 | `GET /api/mypage/reviews`, `user_reviews.target_id` 기준 조회 |
| FR-09-06 | 사용자는 자신의 회원 정보를 수정할 수 있어야 한다. | 필수 | `PATCH /api/mypage/profile` (FR-01-05와 동일 로직 재사용) |

---

## 공통 처리 원칙

| 항목 | 내용 |
| --- | --- |
| 인증 방식 | Spring Security가 이메일·비밀번호 및 카카오·Google OAuth2 로그인을 직접 처리한다. 계정당 하나의 가입 방식만 허용하고 동일 이메일의 다른 Provider 계정을 자동 연결하지 않는다. 서버가 환경변수로 관리하는 공통 비밀키와 HS256(HMAC-SHA256) 알고리즘으로 Access Token을 발급·검증하며, Refresh Token은 해시하여 DB에 저장하고 재발급 시 회전시킨다. JWT 검증 시 허용 알고리즘을 HS256으로 제한하고 `iss`, `aud`, `exp` 클레임을 검증한다. |
| 임베딩 사용 원칙 | 필수 조건(위치/시간/인원 등 명확한 값)은 항상 하드 필터로 처리하고, 벡터 유사도는 "성향/분위기/자유 텍스트"처럼 모호한 선호도를 다루는 보조 랭킹 수단으로만 사용 |
| 장애 격리 | AI/임베딩 관련 기능은 모두 핵심 매칭·채팅 플로우와 분리하여, 실패 시에도 기본 기능은 영향받지 않도록 설계 |
| API 문서화 | Springdoc OpenAPI(Swagger)로 전 엔드포인트 자동 문서화 |
| 모니터링 | Prometheus + Grafana로 매칭 대기열 크기, 매칭 성사율, AI 응답 지연/실패율, WebSocket 연결 수 등 추적 |
